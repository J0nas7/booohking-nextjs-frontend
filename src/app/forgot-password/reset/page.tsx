import { ServerAuth } from '@/layout/ServerAuth'
import { ResetPasswordPage } from '@/views'

const Page = () => (
    <ServerAuth>
        <ResetPasswordPage />
    </ServerAuth>
)

export default Page
